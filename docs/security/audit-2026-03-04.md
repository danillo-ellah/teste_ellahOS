# AUDITORIA DE SEGURANCA — ELLAHOS
**Data:** 2026-03-04
**Auditor:** Security Engineer ELLAHOS
**Supabase Project:** etvapcxesaxhsvzgaane
**Escopo:** Google Drive integration, Edge Functions recentes (job-cast, shooting-day-order, job-timeline, auth-sms-hook), RLS das novas tabelas (job_cast, job_phases, shooting_day_orders), frontend e utilitarios compartilhados

## CONTEXTO

Apos incidente critico de auto-compartilhamento de pastas Google Drive com emails de membros da equipe sem autorizacao explicita do admin, foi solicitada auditoria completa de seguranca. O fix do incidente (auto-share opt-in) foi auditado e confirmado como correto.

## RESUMO EXECUTIVO

A auditoria identificou **0 CRITICOS**, **2 ALTOS**, **7 MEDIOS** e **4 BAIXOS**. A base de seguranca do sistema e solida: JWT validado server-side em todas as EFs auditadas, RLS habilitado e correto nas 3 novas tabelas, CORS dinamico por origem, sem secrets expostos no frontend. Os problemas identificados sao de hardening e defesa em profundidade.

---

## FINDINGS ALTOS

### ALTO-001: Webhook `sync-urls` sem autenticacao

**Arquivo:** `supabase/functions/drive-integration/handlers/sync-urls.ts` (linhas 6-11)
**OWASP:** A01 - Broken Access Control
**Status:** ABERTO

O endpoint `POST /drive-integration/:jobId/sync-urls` e roteado ANTES da chamada a `getAuthContext(req)`. O handler usa `getServiceClient()` (bypass completo de RLS) e NENHUMA verificacao de secret foi implementada.

**Impacto:** Qualquer actor externo que conheca um `jobId` pode sobrescrever `drive_folder_url` do job com URL maliciosa.

**Correcao:** Implementar verificacao do secret `X-Webhook-Secret` com `DRIVE_SYNC_WEBHOOK_SECRET` e fail-closed quando nao configurado.

### ALTO-002: Mensagens de erro interno do PostgreSQL expostas ao cliente

**Arquivos afetados:** 14 handlers em job-cast, shooting-day-order, job-timeline
**OWASP:** A05 - Security Misconfiguration
**Status:** ABERTO

Todos retornam `insertErr.message` que expoe nomes de tabelas, constraints e estrutura interna do banco.

**Correcao:** Substituir por mensagem generica e logar o erro internamente.

---

## FINDINGS MEDIOS

### MEDIO-001: XSS via campos JSONB sem escaping no preview HTML
**Arquivo:** `shooting-day-order/handlers/preview.ts`
**Correcao:** Aplicar `esc()` em `day_number` e `adjustment_minutes`.

### MEDIO-002: JSONB arrays sem validacao de schema dos campos aninhados
**Arquivo:** `shooting-day-order/handlers/update.ts`
**Correcao:** Definir schemas Zod tipados para `crew_calls`, `filming_blocks`, `cast_schedule`.

### MEDIO-003: `job-cast` CREATE/UPDATE/DELETE/LIST sem RBAC
**Arquivos:** `job-cast/handlers/*.ts`
**Correcao:** Adicionar RBAC com `ALLOWED_ROLES` em todos os handlers.

### MEDIO-004: `shooting-day-order` CREATE/UPDATE/DELETE sem RBAC
**Arquivos:** `shooting-day-order/handlers/*.ts`
**Correcao:** Adicionar RBAC com `ALLOWED_ROLES_WRITE`.

### MEDIO-005: Email hardcoded `contas@ellahfilmes.com` em `generate-contracts`
**Arquivo:** `job-cast/handlers/generate-contracts.ts` (linha 340)
**Correcao:** Ler do Vault ou tenant settings.

### MEDIO-006: `owner_email` do Google Drive sem validacao de pertencimento ao tenant
**Arquivo:** `_shared/google-drive-client.ts`
**Correcao:** Validar que `owner_email` pertence a um membro do tenant.

### MEDIO-007: `list-folders` sem verificacao de propriedade do job ao tenant
**Arquivo:** `drive-integration/handlers/list-folders.ts`
**Correcao:** Adicionar verificacao de job ownership antes de listar.

---

## FINDINGS BAIXOS

### BAIXO-001: `setPublicReadPermission` sem mecanismo de revogacao
### BAIXO-002: `transferOwnership` com falha silenciosa sem alerta operacional
### BAIXO-003: Cache de token JWT como singleton de modulo no frontend
### BAIXO-004: Comentario de `auto_share` sugere reativacao sem processo de revisao documentado

---

## AREAS APROVADAS

- **Auth (JWT):** Todas EFs usam `getAuthContext(req)` com validacao server-side. APROVADO.
- **RLS — Tabelas Novas:** `job_cast`, `job_phases`, `shooting_day_orders` — todas com 4 policies e `get_tenant_id()`. APROVADO.
- **CORS:** Lista de origins permitidos, wildcard apenas em endpoints publicos documentados. APROVADO.
- **Frontend:** Sem secrets expostos. Autenticacao correta. APROVADO.
- **Google Drive SA:** Chave privada lida do Vault, nunca exposta. Token expira em 3600s. APROVADO.
- **auth-sms-hook:** Secret validado, OTP nunca logado, telefone sanitizado. APROVADO.
- **FIX-001 Auto-share:** Corrigido corretamente com `=== true` (strict equality). APROVADO.

---

## PLANO DE REMEDIACAO PRIORIZADO

| Prioridade | Finding | Urgencia |
|-----------|---------|---------|
| 1 | ALTO-001 sync-urls sem auth | IMEDIATA |
| 2 | ALTO-002 erro PG exposto | IMEDIATA |
| 3 | MEDIO-001 XSS preview | ALTA |
| 4 | MEDIO-002 JSONB sem schema | ALTA |
| 5 | MEDIO-003 job-cast sem RBAC | ALTA |
| 6 | MEDIO-004 shooting-day-order sem RBAC | ALTA |
| 7 | MEDIO-005 email hardcoded | MEDIA |
| 8 | MEDIO-006 owner_email sem validacao | MEDIA |
| 9 | MEDIO-007 list-folders sem job check | MEDIA |
| 10-13 | BAIXO 1-4 | BAIXA |

**Total:** 13 findings (2 ALTOS + 7 MEDIOS + 4 BAIXOS) + 1 fix confirmado

*Proxima auditoria recomendada: apos correcao dos ALTOS e MEDIOS*
